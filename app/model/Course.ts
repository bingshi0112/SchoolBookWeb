export class Course {
    constructor(public id: string, public university: string, public department: string, public course_num: string
        , public section: string, public prof: string, public semester: string, public teacherAssisstant: string, public description: string) {

    }
}